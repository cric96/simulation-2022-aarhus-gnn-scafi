incarnation: scafi

environment:
  type: Continuous2DEnvironment
  parameters: []

layers:
  - type: CircleLayer
    parameters: [50, 50, 40, 1]
    molecule: info

network-model:
  type: ClosestN #*connectionType
  parameters: [5]

deployments:
  type: Grid
  parameters: [0, 0, 100, 100, 10, 10, 5, 5]